spring:
  datasource:
    # [ ] << 대괄호는 빼면됨
    url: jdbc:mysql://localhost:3306/ssg_tyranno?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}

    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        #        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
  security:
    user:
      name: tyranno
      password: xlfksh97


# swagger
springdoc:
  #  packages-to-scan: global.Swaggerconfig
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: /demo-api.html
    disable-swagger-default-url: true
    display-request-duration: true
    operations-sorter: alpha
