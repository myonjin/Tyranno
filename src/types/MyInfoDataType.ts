export interface MyInfo {
    loginId: string
    name: string
    phoneNumber: string
    email: string
}

export interface ChangeInfo {
    password: string
    phoneNumber: string
    email: string
}
